<div class="container">
    <div class="row flex-nowrap justify-content-between align-items-center">
        <div class="col-12 text-center">
            {% for name, capital in countries %}
                <p>A capital do <b>{{ name }}</b> é <b>{{ capital }}</b></p>
            {% endfor %}
        </div>
    </div>
</div>


